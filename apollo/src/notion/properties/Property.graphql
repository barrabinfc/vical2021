# Abstract Notion Property

enum PropertyTypeEnum {
  title
  rich_text
  number
  select
  multi_select
  date
  people
  file
  checkbox
  url
  email
  phone_number
  formula
  relation
  rollup
  created_time
  created_by
  last_edited_time
  last_edited_by
}

interface Property implements Node {
  id: ID!
  type: PropertyTypeEnum
  name: String!
}
