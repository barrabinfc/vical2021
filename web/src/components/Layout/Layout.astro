<section class="layout">
  <header class="header">
    <slot name="header" />
  </header>
  <div class="main" id="main" tabIndex={-1}>
    <slot />
  </div>
  <footer class="footer">
    <div class="container pad4">
      <slot name="footer" class="container" />
    </div>
  </footer>
</section>

<style lang="scss">
.layout {
  display: grid;
  grid-template-rows: auto 1fr auto;
  grid-template-columns: 100%;
  min-height: 100%;
}

.header {
  position: sticky;
  height: var(--headerH);
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
}

.main {
  display: block;
  transform: translateY(calc( var(--headerH) * -1) );
}

.footer {
  color: var(--text1-invert);
  background-color: var(--surface2-dark);
}
</style>
